export { default as authRoutes } from './routes';
export { auth } from './auth';

// Types are available from Better Auth and Prisma Client
// Better Auth types: import type { Session, User } from 'better-auth/types'
// Prisma types: import type { User, Account, Session, Verification } from '@prisma/client'
